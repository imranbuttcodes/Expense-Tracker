[project]
name = "expense-tracker"
version = "0.1.0"
description = "Async expense tracker MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastmcp>=3.4.5",
]
